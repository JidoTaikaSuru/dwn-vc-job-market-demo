{
  "comment": "VP, OIDC, DIDComm, or CHAPI outer wrapper here",
  "presentation_definition": {
    id: "2aec8c4c-e071-4bda-8a76-41ab27632afa",
    input_descriptors: [
      {
        id: "user has a HasAccount VC issued by us",
        name: "user has a HasAccount VC issued by us",
        purpose: "Please provide your HasAccount VC that we issued to you on account creation",
        constraints: {
          fields: [
            {
              path: [
                "$.credentialSubject.id"
              ],
              purpose: "credentialSubject.id must be did:eth:null",
              filter: {
                type: "string",
                const: "did:eth:null"
              }
            }
          ]
        }
      }
    ]
  }
}
